<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="fill_parent"
    android:layout_height="fill_parent"
    android:background="@color/black"
    android:orientation="vertical" >

    <View
        android:id="@+id/lineView"
        android:layout_width="2dp"
        android:layout_height="match_parent"
        android:layout_alignParentLeft="true"
        android:layout_alignParentRight="true"
        android:background="#000000" >

    </View>

    <View
        android:id="@+id/playerfragmentMenuHeaderBottom"
        android:layout_width="fill_parent"
        android:layout_height="1dp"
        android:layout_margin="2dp"
        android:background="@color/grey" />

    <!-- Editext for Search -->

    <EditText
        android:id="@+id/searchBarPlayerByCategory"
        android:layout_width="fill_parent"
        android:layout_height="40dp"
        android:layout_below="@id/playerfragmentMenuHeaderBottom"
        android:drawableLeft="@drawable/search_icon"
        android:hint="@string/find_player_hint"
        android:background="@color/white"
        android:inputType="textVisiblePassword" />

    <LinearLayout
        android:id="@+id/setting_addplayer_favteam_container"
        android:layout_width="match_parent"
        android:layout_height="50dp"
        android:layout_below="@id/searchBarPlayerByCategory"
        android:orientation="horizontal"
        >

        <ImageView
            android:id="@+id/setting_addplayer_favteam_Logo"
            android:layout_width="45dp"
            android:layout_height="45dp"
            android:layout_marginLeft="10dp"
            android:src="@drawable/addaplayer" />

        <TextView
            android:id="@+id/setting_addplayer_favteam_Header"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_gravity="center"
            android:textColor="@color/white"
             />
    </LinearLayout>

    <ListView
        android:id="@+id/findPLayerByCategoryListView"
        android:layout_width="fill_parent"
        android:layout_height="wrap_content"
        android:layout_below="@id/setting_addplayer_favteam_container"
        android:background="@color/grey_container"
        android:focusable="true" />

</RelativeLayout>